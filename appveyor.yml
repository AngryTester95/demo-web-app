image: Visual Studio 2022

environment:
  PROJECT: 'src/Solution/Solution.sln'

configuration:
  - Debug

before_build:
  - nuget restore %PROJECT%

build:
  project: %PROJECT%
  verbosity: minimal
  
after_build:
  cmd: echo 'after build!'
